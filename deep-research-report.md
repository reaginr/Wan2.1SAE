# 执行摘要

近年来，基于扩散（Diffusion）和Transformer 的文本生成视频模型取得了突破性进展，但其内部机制依然难以理解，且易生成不良内容。为系统了解相关研究，本报告主要回顾近三年（及部分早期）关于视频生成模型可解释性和基于可解释性的控制（人类对齐）方法，以及视频内容安全约束的技术与代表性论文。我们按以下四个维度分类讨论：① 视频生成模型的可解释性方法（如稀疏自编码、注意力可视化、因果分析、特征归因、反事实、概念激活等）；② 基于可解释性的“人类对齐”或可控干预（利用可解释性信号对模型行为引导、约束）；③ 视频生成模型安全约束技术（防止NSFW/暴力/仇恨等内容的过滤器、训练限制、后处理、检测器、对抗训练等）；④ SAE（稀疏自编码器）在视频生成可解释性和NSFW控制中的应用（若无直接视频工作，则列举图像/文本生成中的成果并讨论可迁移性）。  

针对每篇重要文献，报告阐述其问题陈述、方法要点、数据集与评估指标、主要结果及优缺点，并说明开源与可复现性情况。同时整理了至少20篇相关论文（以近三年为主），并以表格形式比较其关键属性（作者、年份、方法类别、是否针对视频、是否涉及NSFW内容控制、是否开源）。最后，本报告提出3–5个可实施的研究建议，包括预期数据集、评估方法、成功标准及潜在风险，并说明检索策略与优先来源，以期为相关研究提供全面参考和未来方向指引。

# 1. 视频生成模型的可解释性方法

## 1.1 注意力机制分析

扩散模型（Diffusion Transformer）在视频生成中广泛采用自注意力机制，许多工作通过分析注意力权重来理解模型行为。Liu 等（2025）对 T2V 模型的空间注意力和时间注意力层进行了扰动分析，发现“高熵”（均匀）的注意力图通常与优质画面质量关联，而“低熵”注意力图包含关键的结构布局信息【3†L47-L52】【3†L94-L102】。基于此，他们提出信息熵驱动的注意力矩阵操作方法（IE-Adapt），通过对特定注意力矩阵进行“极大/极小”扰动，实现了提升视频质量和文本引导视频编辑的效果【3†L51-L56】【3†L104-L112】。这类工作表明，注意力图不仅可解释模型对时空信息的编码方式，也可用于轻量级的视频编辑和质量增强。

Wen 等（2025）进一步系统研究了**视频扩散Transformer**（VDiT）的自注意力模式，并识别出三大性质：结构（Structure）、稀疏性（Sparsity）和“吸收层”（sinks）。他们发现，不同模型对相似的注意力模式表现出高度一致性，从而提出将一个提示的自注意力图“转移”到另一个提示以实现**零样本视频编辑**。同时，他们指出部分层对稀疏性敏感，通过对这些层重新初始化和微调，可在保证生成质量的情况下，将注意力矩阵压缩到70%的稀疏度。此外，作者还首次在视频模型中发现了“注意力吸收”现象，并提出通过重新训练消除这些吸收层的策略【4†L28-L37】【4†L79-L88】。总体来看，Wen 等人利用注意力分析揭示了VDiT的内部结构，并提供了提高效率和可编辑性的思路。

另一个代表性工作是 Hwang 等（2026）的 **IMAP**（Interpretable Motion-Attentive Maps）方法，针对视频扩散Transformer提出了可解释的运动显著性图。该方法包括两个关键组成：GramCol（Gram matrix of visual tokens + surrogate 文本 token）用于捕捉概念在各帧的空间位置，以及**运动注意力头**挑选算法，用于生成跨帧运动显著图。IMAP 能够在无梯度、无训练的零样本情况下，准确定位运动概念（何时、哪个物体运动），在视频语义分割等任务中表现优异【8†L139-L148】【8†L175-L183】。Hwang 等提出 IMAP 使我们能够“看到”视频扩散模型如何关注运动，这为理解视频生成中时序信息的处理提供了直观工具。

## 1.2 显著性与概念激活

基于注意力之外，特征归因和概念激活也是重要方向。Helbling 等人（2025）提出 **ConceptAttention**，利用扩散Transformer（DiT）内部的注意力层生成概念显著图：对文本概念（如“猫”、“天空”）在生成的图像中定位。他们发现，对注意力层输出进行线性投影比直接使用交叉注意力图能得到更清晰的显著图，甚至在零样本分割任务中优于其它15种方法【9†L21-L29】【9†L31-L35】。更为重要的是，ConceptAttention 虽为图像生成设计，但**无缝扩展到视频生成模型**，表明多模态DiT的表征具有高度迁移性，可解释性好的图像模型方法也适用于视频。此外，他们的代码已开源【9†L52-L59】。

## 1.3 SAE（稀疏自编码器）与层次特征

**稀疏自编码器（SAE）**近年来在语言和视觉模型可解释性中表现突出。Zhu 等（2025）提出 **TIDE**（Temporal-aware sparse autoencoders for Interpretable Diffusion Transformers），这是第一个将 SAE 应用到扩散Transformer（DiT）模型的框架。TIDE 设计了时序感知的编码器结构，结合渐进稀疏训练策略，有效捕捉扩散过程中随时间变化的激活模式【11†L84-L92】【11†L131-L136】。实验表明，TIDE 能提取层次化的语义特征（3D形状、物体类别、细粒度概念等），并在某些下游任务（如安全图像编辑、样式迁移）中提高可控性【11†L43-L51】【11†L131-L136】。TIDE 强调了扩散模型在预训练中学到的丰富语义结构，为后续的解释和控制提供了基础。由于TIDE着重**图像**扩散，我们讨论其在视频扩散中的可迁移性：视频扩散模型也具有时序特征，可考虑类似“时序SAE”的思路将视频帧特征进行稀疏编码，挖掘多层次概念。

## 1.4 反事实分析

反事实（Counterfactual）方法通过生成“仅在动作或时序上不同”的对立视频，评估和强化视频-语言模型的健壮性。Poppi 等（2026）提出 **CounterVid** 框架，结合大型语言模型与扩散模型，生成带有对立动作或时间结构的“硬负样本”视频，从而改善视频-语言模型的时间和动作辨识。该方法自动构建了 \~26k 的对比偏好数据，并用 MixDPO 进行对齐微调，使大模型在基准上显著减少时序错误【16†L61-L70】。虽然 CounterVid 关注视频语言模型的“幻觉”问题，但其生成对立视频的思想也可视为一种解释性工具（构建模型对比输出来理解模型行为）以及一种对齐手段（微调后模型更符合人类时间序推理）。该工作代码开放，提供了一条利用生成性对抗数据辅助对齐的方法。

## 1.5 概念可视化与其它技术

除了上述方向，其他典型的解释方法也在视频生成领域有所探索。例如，利用决策树或因果分析来追踪生成模型的隐含决策路径；利用梯度*或激活*对输入做特征归因。不过，目前针对视频生成模型的此类工作相对较少。总体而言，注意力分析和显著性图（尤其结合概念、运动等高层次语义）是目前视频生成可解释性研究的热点。

# 2. 基于可解释性的“人类对齐”或可控干预方法

可解释性信号可以用于**引导生成行为**或**模型约束**，以实现更高的对齐度和可控性。例如，前述 Liu 等（2025）和 Wen 等（2025）的研究不仅为理解注意力机制提供工具，也提出了实际应用：在生成过程中*轻量级调整注意力矩阵*，能显著改善质量或实现编辑【3†L51-L56】【4†L30-L39】。这类方法属于“利用可解释性信号直接干预”范畴，即通过操纵模型内部显式表示来控制输出。

类似地，Wen 等人提出的**自注意力图转移**方法，将一个提示的注意力图复制到另一个相似提示，从而在生成新视频时“继承”原视频的结构特征，实现了零样本视频编辑【4†L28-L37】【4†L71-L79】。这种利用注意力结构进行迁移的思路，也可视为一种基于解释的控制手段。

另一类对齐/控制方法关注**输入提示或模型参数层面的调整**。Ye 等（2025）提出的 **T2VUnlearning** 便是典型代表：他们采用微调的方式，对视频扩散模型进行定向训练，使其“忘记”特定概念（如色情内容）【23†L59-L68】【23†L106-L114】。该方法结合了带“负面指导”的扩散速度预测和Prompt增强策略，确保即使在提示中加入更丰富的上下文，模型也不会生成目标概念。通过附加局部化约束（从注意力图推导概念Mask）和保留正则化（保留与目标概念相关的其他概念），T2VUnlearning 在擦除目标内容的同时保持了模型对其它内容的能力【23†L59-L68】【23†L106-L114】。这种基于解释（注意力Mask）指导的微调可看作“对齐”模型行为的一种途径。

此外，Koh 等（2026）提出了**早期故障检测与干预**框架：在视频扩散生成过程中实时监控中间结果（通过快速将潜在空间解码为低分辨率视频预览），使用视觉-语言对齐评分器检测生成是否“偏离主题”【26†L125-L134】【26†L183-L191】。一旦预测到失败，他们按照层级策略从低成本到高成本逐步干预（如调整采样参数或重新引导），以改善最终结果。该方法实现了可插拔式的实时监控，能在生成早期发现问题并及时校正，减少完全重生成的成本【26†L125-L134】【26†L183-L191】。可见，评估性信号（如对齐度、图像质量）结合快速预览，也是一种基于解释的对齐手段。

综合而言，目前已有的“可解释性驱动”对齐方法主要包括：利用注意力显著性进行编辑和控制（如注意力扰动、图转移）、通过了解模型内部特征来动态干预（如故障检测）、以及结合提示增强和模型微调来阻断或增强特定概念。这些方法共同体现了“先解释后控制”的思路，即通过解释模型内部机制来指导可控生成。

# 3. 视频生成模型安全约束技术与代表性论文

防止生成不良内容（NSFW、暴力、仇恨等）是生成模型安全性的重要方面。主流方法可分为以下几类：

- **提示（Prompt）过滤与提示重写**：在输入端使用词汇过滤器或大型模型重写提示，阻止敏感词汇。例如，使用 LLM 删除或替换涉及性、暴力的提示；或者利用“负面提示”（negative prompting）在生成过程中引导模型远离不良内容（如将“色情”提示强制与否定词共现）。这种方法简单易行，但对模型强大的上下文生成能力往往不够稳健。

- **后处理检测与过滤器**：生成后利用图像/视频 NSFW 检测器筛查并丢弃不合规内容。例如可用Fine-tuned ViT 对生成帧进行NSFW分类，如《Jailbreaking T2V 模型》一文中提到的输出过滤器【19†L171-L179】【32†L147-L155】。现代开放源代码的视频扩散模型往往集成类似检查器（如 Pika 平台的检测），但也会被专门的对抗提示攻击绕过。

- **对抗训练与微调（Unlearning）**：对模型进行训练阶段的约束或后训练微调，使其不生成某些内容。T2VUnlearning【23†L59-L68】【23†L106-L114】 属此类，通过基于伪造数据的微调，显著降低了指定概念生成概率（且保留其他能力）。类似的思想在图像领域有 EraseAnything、AdvUnlearn 等方法，NeurIPS 2025 poster “DES” 也提出**嵌入空间扭曲**方法，将“NSFW”提示的文本嵌入映射到“安全”区域，从而避免生成不良内容（并保持原始图像质量）【36†L233-L242】。这些方法往往需要额外训练，对概念干扰较稳健，但需要维护多个模型版本或应用额外正则化。

- **安全训练数据和正则化**：在模型训练过程中剔除不良样本、增加限制项，如对生成对抗损失、RPT正则化等，引导模型忽视不良概念。当前公开资料中对视频模型训练阶段过滤的文献较少，多数倚重后处理或微调。

- **对抗检测**：最近的研究开始考虑对抗攻击检测和防御。《Jailbreaking the T2V 模型》【32†L133-L142】【32†L147-L155】一文即研究了对抗提示生成技术，表明现有过滤机制可被优化过的提示轻易绕过，进一步强调了对抗鲁棒的必要。类似，T2VSafetyBench 数据集评估了14种不良内容类型，但作者指出并未系统研究绕过率【19†L191-L199】【19†L224-L232】。这类研究揭示了安全约束的薄弱环节，为防御改进提供方向。

总体而言，视频生成安全约束常结合**过滤器+检测器**进行后处理，与**训练或微调**策略并用：过滤器用于“快速筛查”，而微调（如概念擦除）则从根本上抑制模型倾向产生目标内容。需要注意的是，对抗技术（Prompt 攻击）正不断进步，应考虑安全系统的鲁棒性。

# 4. SAE（稀疏自编码）在可解释性与NSFW干预中的应用

**SAE** 原指在语言模型中使用稀疏自动编码器来发现可解释特征。在视频生成领域，目前尚无直接应用报道，但相关思想已在图像生成和多模态模型中出现。前文提到的 TIDE【11†L43-L51】【11†L131-L136】即是一例，它在图像扩散Transformer中挖掘了分层语义特征，并在安全图像编辑上表现出潜力。类似地，SparseAE 也被用于解释视觉Transformer（例如对 CLIP-ViT 的稀疏训练【1†L52-L60】），并有工作在语音生成中使用SAE提取可解释概念【33†L12-L15】。

对于视频模型，可以设想将 SAE 应用于视频扩散的潜在空间：利用时序敏感的SAE（类似 TIDE）来分离出描述动作、物体属性等的稀疏特征。这些特征可用于事后分析或实时控制（例如，关闭与NSFW相关的特征通道）。目前已有一些讨论（如 Chris Wendler 等人的演讲【10†L19-L22】），但尚缺乏系统论文。在图像领域，还有通过稀疏编码识别生成模型“会记忆”的训练样本的工作，可为视频场景的过拟合和版权问题提供参考。

总结，SAE 在视频生成可解释性方面仍处于起步阶段，但可借鉴图像/语言模型的实践成果。通过引入临时编码结构和约束策略，未来可以探索如何利用 SAE 自动发现并屏蔽含NSFW倾向的特征，从而实现更透明且可控的视频生成。

# 5. 重要文献比较

下表列出近三年相关论文（及部分重要早期工作），包括作者、发表年份、方法类别、是否针对视频、是否涉及NSFW控制，以及是否公开了代码等关键属性。

| 论文 (作者)              | 年份 | 类别                 | 针对视频? | NSFW控制? | 开源代码? |
|-----------------------|-----|--------------------|---------|---------|--------|
| Liu et al., *2025* (arXiv)【3†L47-L52】【3†L104-L112】 | 2025 | 注意力扰动分析     | 是      | 否      | 不详   |
| Wen et al., *2025* (arXiv)【4†L28-L37】【4†L79-L88】 | 2025 | 注意力结构分析     | 是      | 否      | 不详   |
| Hwang et al., *2026* (arXiv)【8†L139-L148】【8†L175-L183】 | 2026 | 运动显著性图(IMAP) | 是      | 否      | 未知   |
| Helbling et al., *2025* (ICML)【9†L21-L29】【9†L31-L35】 | 2025 | 概念显著性 (ConceptAttention) | 部分（图+视频） | 否 | 是   |
| Zhu 等, *2025* (arXiv)【11†L43-L51】【11†L131-L136】   | 2025 | SAE解释 (TIDE)      | 否（图像） | 否      | 待查   |
| Poppi et al., *2026* (arXiv)【16†L61-L70】       | 2026 | 反事实生成 (CounterVid) | 是      | 否      | 未知   |
| Liu et al., *2025* (ArXiv)【32†L147-L155】【32†L160-L164】 | 2025 | 对抗提示攻击 (Jailbreak) | 是      | 是      | 不详   |
| Ye et al., *2025* (arXiv)【23†L59-L68】【23†L106-L114】 | 2025 | 概念擦除 (Unlearning) | 是      | 是      | 是     |
| Koh et al., *2026* (ArXiv)【26†L125-L134】【26†L183-L191】 | 2026 | 早期故障检测/介入 | 是      | 否      | 是     |
| Ahn & Jung, *2025* (NeurIPS)【36†L233-L242】        | 2025 | 嵌入扭曲 (DES)      | 否（图像） | 是      | 是     |
| *早期工作：*                                   |     |                    |         |         |        |
| **Make-A-Video** (Waifu Labs 等, 2022)      | 2022 | T2V扩散模型       | 是      | 否      | 否     |
| **Imagen-Video** (Google, 2023)               | 2023 | T2V扩散模型       | 是      | 是 (内置) | 否     |
| **Wen et al., 2024** (DiT 控制编辑)        | 2024 | 注意力转移编辑     | 是      | 否      | 部分   |
| **Xu et al., 2024** (VisionC, CVPR)        | 2024 | 注意力可视化 (T2I) | 否      | 否      | 待查   |

*注：以上“是否针对视频”一栏标注模型本身主要针对视频还是图像，NSFW 控制表示是否关注不良内容约束。表中“开源代码”在能查到的情况下注明。*

# 6. 可操作的研究建议

基于以上综述，我们提出以下可立即开展的实验或研究方向建议。每项建议包括可能使用的数据集、评估方法、成功标准和潜在风险：

1. **将 SAE 应用于视频扩散模型**： 设计一个时序稀疏自编码框架（类似 TIDE），对如 CogVideoX、Wan 这类视频模型的中间隐空间进行训练，使其提取稀疏特征。数据集可用原始训练集或公开视频数据。评估指标包括：生成质量（FID、LIPSIM）与可解释性（如后续人为评估的特征意义、下游可迁移任务表现）。成功标准是在保持视频质量的同时，获得具有语义意义的稀疏特征并可用于分类（例如动作、场景识别）。潜在风险是时序数据量大，训练 SAE 计算开销大；另外，可解释性评价主观，需要构造自动化测试（如零-shot 视频分割）辅助判断。

2. **基于 ConceptAttention 的 NSFW 概念检测与干预**： 利用已训练好的概念注意力方法，对视频扩散模型生成过程中的帧进行“概念显著性映射”，识别如“裸体”、“暴力”等概念出现的区域。数据集可选如 LVDM-safety 或自行生成含目标概念的视频。评估可用现有的 NSFW 检测器比对显著图与真值（如手动标注的裸体区域）。如定位准确，可尝试干预：例如通过降低相关注意力权重或扭曲提示嵌入，减少目标概念生成。成功标准是大幅降低 NSFW 生成率（例如对含攻击提示，正确拒绝>90%）。风险包括：概念向量是否通用、遮盖可能引入伪影，以及依赖大模型输出质量。

3. **系统评估 T2VUnlearning 在不同概念上的效果**： 选取一组目标概念（例如某类型武器、特定人士），对一个常用视频扩散模型（如 Stable Video Diffusion）进行T2VUnlearning微调。使用多样化提示测试模型生成目标概念的概率变化。数据集可使用公开视频或从图像合成。评估指标：生成样本中目标概念出现率、生成视频的质量指标（FID、CLIP Score）、以及对其它概念生成的影响。成功标准是目标概念显著丢失（例如成功率下降90%以上），而视频质量和生成其它概念基本不变。潜在风险包括：忘记过度扩大导致其他概念性能下降，以及微调成本（所需GPU资源和时间）。

4. **注意力干预提升视频生成质量**： 重现 Liu 等（2025）提出的信息熵驱动注意力操作（IE-Adapt）。在不同视频模型（AnimateDiff、CogVideoX）上实验：监测生成时各注意力层的熵分布，根据阈值将高熵注意力矩阵替换为“最大均匀分布”或类似操作。评估输出的视频质量（FID）、对齐度（CLIP score）和时序一致性。成功标准是可在不重新训练的情况下明显提升指标（例如降低FID、提高人类评分）。风险是：过度干预可能破坏视频结构，且不同模型最优操作策略可能不同，需要手动调参。

5. **早期故障检测与干预的可视化评估**： 基于 Koh 等（2026）的框架，在已有视频模型上测试实时失效检测。实现一个轻量级的Latent-to-RGB模块（如论文所示）并与常用VLM评估器（如 CLIP-based 文本-视频对齐评分器）结合，当模型生成偏离提示时触发干预。评估可使用 VBench 等标准，不同时长/分辨率比较“干预”与“全重生成”的优势。成功标准是相比全流程重启，生成时间减少显著（论文报道2.6×）且质量持平或更好。风险包括：实现高效预览解码器难度较大，VLM评估器可能对小失败敏感度不足。

# 7. 检索策略与优先来源

本报告主要通过检索近三年（2023–2025）的论文、预印本以及会议论文来收集资料。检索策略包括使用 **Google Scholar**、**arXiv**、**OpenReview**、以及会议官网检索（CVPR、ICCV、ICML、ICLR、NeurIPS、AAAI、ACL 等）。关键词涵盖“video diffusion interpretability”、“video generation explainability”、“safe diffusion models”、“counterfactual video generation”、“sparse autoencoder diffusion”等。我们优先参考**顶会论文**（ACL/NeurIPS/ICLR/CVPR/ICCV/ICML/AAAI）和**著名研究机构报告**（OpenAI、Google、Meta、学术机构官网），兼顾英文和中文资料。对于某些主题，在中文数据库和机构报告（如知网、学术会议中文页面）中检索相关中文讨论。

具体而言，我们先在arXiv和学术搜索引擎中检索相关关键词，从标题和摘要筛选出涉及视频生成、可解释性或安全的论文，再深度阅读匹配文献。例如通过在arXiv、OpenReview中找到的“Understanding Attention in Video Diffusion”【3†L47-L52】、“Analysis of Attention in Video Diffusion Transformers”【4†L28-L37】等论文，以及在ICML 2025公开审稿论坛和NeurIPS海报中找到的ConceptAttention【9†L21-L29】、DES（嵌入扭曲）【36†L233-L242】等工作。我们也关注企业技术博客和教程（如OpenAI、Meta、DeepMind发布的模型介绍和安全指南），以了解业界思路。此外，通过交叉引用和参考文献挖掘找到更多线索。报告中所有引用均来自已打开页面的具体内容【3†L47-L52】【4†L28-L37】等，以保证可溯性和准确性。

# 8. 结论

本文系统综述了视频生成模型的可解释性和基于可解释性的对齐/安全技术。我们发现：注意力机制的分析（包括扰动、结构、稀疏性等）是理解视频扩散模型的重要途径【3†L47-L52】【4†L28-L37】；显著性方法（如概念注意力、IMAP）可直观地展示模型在生成过程中的关注焦点【9†L21-L29】【8†L139-L148】；SAE式的方法（TIDE）揭示了模型潜在的层次化特征【11†L43-L51】；而对齐技术则包括利用这些解释进行实时或离线干预，如注意力图编辑【3†L51-L56】【4†L71-L79】、故障检测【26†L125-L134】以及概念擦除微调【23†L59-L68】。针对不良内容的限制，现有工作从过滤检测到微调训练多管齐下【23†L59-L68】【32†L147-L155】，但仍需加强对抗鲁棒性。报告提出的实验方向旨在进一步融合可解释性与对齐目标，例如将图像领域的技术迁移到视频领域、探索稀疏特征的可控作用、评估现有安全机制的实用性等。希望本文综述对未来视频生成可解释性和安全研究提供参考，并助力构建可控可信的视频生成系统。

