# Wan2.1 T2V 1.3B 模型详细架构解析（修正版）

**版本**: v2.0 (根据用户反馈修正)
**日期**: 2026-03-29

---

## 关键修正声明

### 修正1: "编码视频"的误解

**原文档错误**: 提到"编码视频"
**实际情况**: Wan T2V (文生视频) 推理时**只有文本输入**，没有视频输入

**正确流程**:
```
推理阶段:
文本提示 → T5编码 → 随机噪声(初始) → DiT去噪(50步) → VAE解码 → 81帧视频

训练阶段才需要:
真实视频 → VAE Encoder → Latent → 加噪 → DiT学习 → 重建视频
```

---

## T2V 1.3B 核心参数

| 参数 | 值 |
|------|-----|
| 模型维度 (dim) | 1536 |
| FFN 维度 | 8960 |
| 注意力头数 | 12 |
| **DiT 层数** | **30层** (固定) |
| 采样时间步 | 30-50步 (可调) |
| 生成帧数 | 81帧 |
| 分辨率 | 832×480 |
| T5输出 | [1, 512, 4096] |

---

## 完整流程示意图

```
【输入】文本: "一只猫在草地上奔跑"
         ↓
T5编码 → [1, 512, 4096] → Linear → [1, 512, 1536]
         ↓
【初始化】随机噪声 → [1, 16, 21, 60, 104]
         ↓
═══════════════════════════════════════════════════════
【DiT去噪 - 循环50次】
对于每个时间步 t:

    PatchEmbed → [1, 32760, 1536]
         ↓
    ┌─────────────────────────────────────┐
    │ 循环30层 (Block 0 → Block 29):      │
    │                                     │
    │  每层结构:                          │
    │  1. Self-Attention (时空全局)       │
    │     - Q,K,V来自视觉                 │
    │     - 3D RoPE位置编码               │
    │     - 帧1可以直接看到帧81!          │
    │                                     │
    │  2. Cross-Attention (文本条件)      │
    │     - Q来自视觉, K,V来自文本        │
    │     - 注意力矩阵: [32760, 512]      │
    │     - 这是文本影响生成的唯一位置!   │
    │                                     │
    │  3. FFN (前馈网络)                  │
    │     - Linear(1536→8960)→GELU        │
    │     - Linear(8960→1536)             │
    │                                     │
    │  每层都有adaLN-Zero调制 + 残差连接  │
    │                                     │
    └─────────────────────────────────────┘
         ↓
    Head输出 → 预测噪声 v_pred
         ↓
    CFG: v_final = v_uncond + 5.0 × (v_pred - v_uncond)
         ↓
    调度器: latent_{t-1} = latent_t - step × v_final

═══════════════════════════════════════════════════════
【VAE解码】
最终latent [16,21,60,104] → Causal 3D VAE → 81帧视频 [3,81,480,832]
```

---

## 关键问题解答

### Q: 30层 DiT vs 50个时间步的关系？

**答案**: **30层 ≠ 50步**

```
时间步50: latent_50 → [Block0→...→Block29] → v_pred → 调度器 → latent_49
时间步49: latent_49 → [Block0→...→Block29] → v_pred → 调度器 → latent_48
   ...
时间步0:  latent_0  → [Block0→...→Block29] → v_pred → 调度器 → latent_final

注意: 同样的30层Block被复用了50次!
     就像工厂的30道工序，原料在变，工序不变。
```

---

### Q: 81帧是逐帧生成还是并行生成？

**答案**: **并行生成所有81帧**

```
Wan2.1 T2V不是逐帧生成!

初始噪声就包含所有帧: [16, 21, 60, 104]
                              │
                              └── 21个时间位置对应81帧

DiT处理时:
- 所有帧的patches被展平在一起: [32760, 1536]
- 自注意力允许任意帧间直接交互
- 输出仍然是完整的时空立方体
```

**与自回归生成的区别**:

| 方式 | Wan2.1 T2V | 自回归视频 |
|------|-----------|-----------|
| 机制 | 并行生成 | 逐帧顺序 |
| 帧间关系 | 全局自注意力双向 | 前一帧作为后一帧输入 |
| 一致性保证 | 3D RoPE + 全局注意力 | 天然时序因果 |

---

### Q: DiT输出的是什么？

**答案**: **预测噪声 v_pred**，不是去噪后的图像!

```
Flow Matching框架:
- DiT学习预测: 当前噪声状态应该往哪个方向走
- 调度器(Scheduler)负责: 根据方向走一步

类比:
- DiT = 导航仪 (告诉你要往哪走)
- 调度器 = 方向盘 (实际执行转向)
```

---

### Q: 文本如何影响生成？

**答案**: 通过**Cross-Attention**在每个Block中影响。

```
Cross-Attention机制:
Q (Query):  [32760, 1536] ← 视觉特征 "我需要什么信息?"
K (Key):    [512, 1536]   ← 文本特征 "我有什么信息?"
V (Value):  [512, 1536]   ← 文本特征 "信息内容是什么?"

注意力矩阵: [32760, 512]
- 每个视觉位置关注所有512个文本token
- "猫"的权重高 → 生成猫的特征
- "草地"的权重高 → 生成绿色纹理
```

---

## SAE创新应用（24GB显存优化）

### 当前显存分析

```
Wan 1.3B模型: ~2.6GB
VAE:          ~0.5GB
T5:           ~2.0GB
激活值:       ~15-20GB (主要开销)
─────────────────────────────
总计:         ~24GB (接近极限)
```

### 推荐优化方案

#### 方案1: 时间步稀疏采样
```python
def select_key_timesteps(mode="boundary"):
    if mode == "boundary":
        return [0, 25, 49]    # 只选3个关键步
    elif mode == "uniform":
        return [0, 10, 20, 30, 40, 49]

# 显存节省: ~94%
```

#### 方案2: 层间稀疏采样
```python
selected_layers = [15]  # 只训练第15层
# 显存节省: ~97%
```

#### 方案3: 离线特征采集+训练（推荐）
```python
# 阶段1: 采集特征 (无梯度)
for prompt in prompts:
    for t in timesteps:
        with torch.no_grad():
            feat = hook_features().cpu()
            save_to_disk(feat)

# 阶段2: 离线训练SAE (纯SAE，无DiT)
features = load_from_disk()
sae = SparseAutoEncoder(1536, 3072)
```

### 推荐的SAE配置

```python
optimal_config = {
    "d_model": 1536,
    "d_hidden": 3072,       # 2倍扩展(原4倍)
    "top_k": 32,            # 减少k值
    "strategy": "offline",  # 离线训练
    "hook_layers": [15],    # 只训练中层
    "key_timesteps": [0, 25, 49],
    "batch_size": 4096,     # 离线可用大batch
}
# 预期显存: ~15GB，留有9GB余量
```

---

## Q&A 问答区

### 已回答问题

| # | 问题 | 答案 |
|---|------|------|
| 1 | "编码视频"? | 推理时无视频输入，从随机噪声开始 |
| 2 | T5输出维度? | [1,512,4096]，单维度无语义，分布式表征 |
| 3 | DiT块内部? | adaLN→SelfAttn→CrossAttn→FFN |
| 4 | 文本如何影响? | Cross-Attention，Q视觉/KV文本 |
| 5 | DiT输出? | 预测噪声v_pred，不是图像 |
| 6 | 下一时间步输入? | 上一步更新后的latent |
| 7 | 走完输出什么? | 完整81帧latent，并行生成 |
| 8 | 如何保持连续? | 3D RoPE + 全局自注意力 |
| 9 | 81帧如何解码? | Causal VAE聚合解码 |
| 10 | 30层vs50步? | 30层Block复用50次 |
| 11 | 24GB优化? | 时间步稀疏+层稀疏+离线训练 |

### 待补充问题区

- Q12: [待用户添加]
- Q13: [待用户添加]

---

**文档结束**

*本文档已根据用户反馈进行关键修正，重点澄清了T2V推理流程、DiT层与时间步的关系、并行帧生成机制。*
