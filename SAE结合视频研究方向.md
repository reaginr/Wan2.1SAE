# 执行摘要

目前尚未发现**直接使用SAE（稀疏自编码器）解释视频扩散Transformer（Video DiT）**内部激活以实现干预/控制的文献。已出现的相关工作主要聚焦于图像生成或多模态模型中使用SAE：如Huang等人提出的**TIDE**（图像DiT）【38†L57-L64】和Rao等人的**Discover-then-Name**（CLIP概念发现）【44†L73-L82】都利用SAE自动挖掘模型的可解释特征；Pach等人（NeurIPS 2025）进一步表明，训练SAE可显著增强视觉-语言模型（如CLIP）的单语义神经元特征，并可通过调整CLIP编码器直接引导多模态LLM输出【49†L239-L247】。此外，还有一些与解释和控制相关的方法：如Helbling等提出的**ConceptAttention**（DiT概念显著性映射）【9†L21-L29】、Hwang等人的**IMAP**（视频运动显著图）【8†L139-L148】、以及Liu等和Wen等对视频注意力结构的分析【3†L47-L52】【4†L28-L37】。在安全控制方向，有Ye等的**T2VUnlearning**通过微调擦除视频扩散模型中指定概念【23†L59-L68】，以及Poppi等的**CounterVid**利用反事实视频辅助对齐模型【16†L61-L69】。本文回顾了上述核心工作（对每篇给出问题、方法、数据集、结果等要点）并评估其代码开放性与可迁移性。我们认为可将TIDE、Rao等在图像/多模态领域的SAE技术迁移到视频：需要引入时序模块（如3D卷积或时序Transformer）以同时稀疏化帧间特征；并结合视频特有的数据和计算成本。为了设计**时序SAE**，我们总结了可能的架构改进（时序自编码器、时序稀疏化、时序注意力、时空卷积、因果卷积、Transformer 变体、时序对比学习、动态稀疏训练等），并分别分析了它们的优缺点、适用场景和实现要点。我们还列出至少10篇近三年相关论文并对比其关键属性。最后提出了若干可操作实验建议（如在小规模视频数据上训练时序SAE、测试SAE特征对特定概念的抑制能力、利用概念注意力定位并干预不良内容等），并说明了检索方法与核心文献，以供进一步研究参考。

## 1. SAE在视频DiT可解释性中的应用

目前**未检索到专门针对视频DiT的SAE解释工作**。最接近的是图像领域和视觉-语言模型中的研究。例如，Huang 等人提出的**TIDE**框架（Temporal-aware sparse autoencoders）使用SAE提取图像生成模型（DiT）的稀疏激活特征【38†L57-L64】。TIDE通过在扩散的多个时间步捕获层次语义（如3D结构、物体类别）来增强可解释性，并在安全编辑等任务上表现良好【38†L57-L64】。Rao 等（ECCV 2024）提出的方法则利用SAE在CLIP模型中自动发现概念，并构建概念瓶颈模型【44†L73-L82】。Pach 等（NeurIPS 2025）展示了将SAE应用于视觉-语言模型（如CLIP），能够**极大提升单神经元的单义性**（monosemanticity），并通过在CLIP编码器中施加SAE干预来无修改地**引导多模态生成模型输出**（如LLaVA）【49†L239-L247】。这些工作均说明SAE能有效挖掘深度模型的语义特征，并实现某种程度的控制。

对于视频DiT，可将上述思想迁移：首先，视频扩散模型具有**时序维度**，因此SAE编码器需结合时空信息。例如，可将TIDE的时序编码扩展为**3D卷积或时序Transformer结构**，在压缩激活的同时保留帧间关联。TIDE表明DiT在预训练中学到了层次语义，这提示视频模型也可能自动学到运动和场景等概念，因此理论上可通过训练SAE来分离这些概念。然而，**直接训练视频SAE的计算成本**较高：视频模型具有更长的上下文（帧长）、更大的参数和更多计算，训练SAE时需注意内存和效率。此外，视频生成任务下数据稀缺度不同，可能需要专门的视频数据集（如WebVid、UCF101）来学习时序特征。实现上，可以借鉴视频自编码技术：例如，**ConvLSTM自编码器**或**3D卷积自编码器**可作为编码器/解码器的基础，以处理连续帧的输入。

除了SAE，其他可解释方法可为视频DiT提供参考。例如**ConceptAttention**【9†L21-L29】使用扩散模型的注意力层生成概念显著图，可定位文本概念在图像中的位置；Hwang等的**IMAP**专门针对视频DiffusionTransformer提出运动概念的时空显著图【8†L139-L148】；Liu等和Wen等则通过对视频扩散模型的注意力进行扰动与结构分析，实现了可控编辑【3†L47-L52】【4†L28-L37】。这些方法虽不使用SAE，但表明利用模型内部激活（如注意力或概念嵌入）可以实现**对生成过程的干预**（比如编辑图像、强化质量、消除错误）。

**总结**：虽然我们未找到直接针对视频DiT使用SAE进行解释/控制的工作，但图像/多模态领域已有多种相关方法（TIDE【38†L57-L64】、Rao等【44†L73-L82】、Pach等【49†L239-L247】、ConceptAttention【9†L21-L29】、IMAP【8†L139-L148】等）。它们揭示了利用SAE或注意力等机制提取可解释特征并引导模型输出的可能。将这些思路迁移至视频，需引入时序机制（3D卷积、时序Transformer等），并考虑视频数据量与计算资源。下文第二部分将详细讨论设计**时序SAE**的可选架构和技术。

## 2. 设计“时序SAE”的可参考架构与改进

为了构建适用于视频生成模型的SAE编码器，需要在时序信息处理方面做出设计。以下列出几种相关的架构思路，并讨论其优缺点、适用场景与实现要点：

- **时序自编码器**：采用RNN/ConvLSTM等结构编码视频序列。优势是可自然处理任意长度序列，对长期依赖建模能力较好；缺点是计算速度较慢、难并行。常见方式是将连续帧输入一个ConvLSTM自动编码器，再重构整个帧序列。如Ruan等用于视频异常检测的两流自编码网络，用**Conv-LSTM**学习帧间关联【63†L219-L228】。场景：较小视频或下游任务（异常检测）。实现要点：确保序列长度和状态记忆容量匹配，可能需截断视频或分段训练。

- **时空卷积（3D卷积网络）**：将2D卷积扩展到3D卷积，直接捕捉视频的时空信息。优点是并行度高，可提取局部运动模式（卷积核覆盖多帧）；缺点对大时序范围捕捉有限，参数量增多。【66†L1-L4】指出，传统U-Net视频生成架构就是在空间卷积后插入时间注意力，并将卷积拓展到3D以建模时序动态【66†L1-L4】。适用于需要直接处理短时依赖的视频，且对实时性要求高场景。实现：可以基于现有3D-AE（如SlowFast网络【63†L197-L205】）构建编码器，解码器使用反向时空卷积。

- **因果卷积（Causal Conv）**：类似WaveNet的1D时序卷积，保证只依赖过去帧。优点是天然顺序处理、并可并行推理（通过掩码）；缺点相对缺乏全局视野，难捕捉长程关联。适合对连续预测和逐帧生成（如逐帧解码策略）有需求的场景。实现：可用膨胀卷积（Dilated Conv）增大感受野，或结合全连接层生成特征。

- **Transformer 编码器/解码器**：利用自注意力捕捉整个序列的全局依赖。当前视频Transformer（如VideoTransformer、TimeSformer）的自注意力可以兼顾空间和时间维度，灵活性高。缺点是计算复杂度高（时序长度平方），且对数据量要求大。可参考Tsformer的时间注意力机制。对于时序SAE，可设计**时空Transformer编码器**（交替的空间注意力和时间注意力），如**TimeSformer**模式，或将空间帧切分后按时间维度串联输入编码器。实现要点：注意分离或共享位置编码、内存优化（可使用窗口自注意力或分层注意力减少开销）。

- **时序注意力**：指专门关注帧间动态的注意机制。SparseVideoGen【53†L108-L117】观察到视频DiT的注意力头分为空间型和时间型：**空间头**仅关注同一帧内的token，而**时间头**主要关注相同空间位置跨帧的token【53†L108-L117】。这种发现提示，可以设计双流结构：一部分网络专门做空间自注意，另一部分做时间自注意，然后合并。这种结构对视频生成很自然：空间流理解场景内容，时间流捕捉运动一致性。实现：在Transformer中用分类标签明确分配注意头，或像SVG所做的在线采样决定每个头是空间还是时间【53†L108-L117】。

- **时间对比学习**：利用自监督目标增强时序特征学习。例如Wang等的**LSTCL**（Long-Short Temporal Contrastive Learning）强调对比同视频的长视图和短视图，使模型学习捕捉视频的长程上下文【64†L1-L5】。尽管该方法用于视频理解预训练，其思想可用于SAE：将视频切为不同长度片段，通过对比任务引导Encoder提取一致的时序特征。优点是无需人工标注，可增强时序一致性；缺点需额外预训练步骤且收敛慢。场景：可以先对视频自编码器进行对比预训练，再微调重构。实现：设计双分支网络，一支处理长序列、一支处理短序列，同视频内最大化特征相似度。

- **动态稀疏训练**：训练过程中逐步稀疏化网络结构或输入。Wu等提出的**DynST**虽然针对时空传感数据，但其动态稀疏思想可启发视频模型：在训练时动态去除对预测贡献小的时序特征【60†L42-L50】。优点是节约计算、提升效率；缺点可能导致信息丢失、模型稳定性问题。实现时可在训练中引入逐步剪枝策略：每隔一定step计算激活重要性，并剪掉低贡献帧或通道。需要注意保持时序对齐，避免过度剪枝导致表达丧失。

以上架构各有优劣，应根据具体需求选择。例如**ConvLSTM自编码器**易于实现且对短序列有效，【63†L219-L228】的两流模型即采用此法；**3D卷积/Transformer**对时空关联建模能力强，但需大规模训练；**注意力分离**结构（空间/时间注意头）能够精细提取不同类型信息【53†L108-L117】；**对比学习**可提升表征鲁棒性；**动态稀疏**有助于减负但需谨慎设计。下表列出各架构要点对比：

| 架构/方法            | 优点                                    | 缺点                                    | 适用场景                      | 实现要点                                                    |
|--------------------|---------------------------------------|---------------------------------------|---------------------------|-----------------------------------------------------------|
| **时序自编码器** (ConvLSTM) | 适合捕捉短时关联、并行度高                  | 长程依赖捕捉较弱、序列长度限制               | 小型视频重建、异常检测等             | 使用卷积LSTM编码，加入隐态传播；按帧输入，可并联处理小批量        |
| **时空卷积** (3D Conv)      | 并行处理、参数化小、可直接提取局部时空特征        | 对长程依赖建模能力有限、计算量大（3D卷积）      | 高速性要求的实时生成、简单场景          | 基于3D卷积或SlowFast网络设计编码器；正则化训练防止过拟合           |
| **因果卷积** (1D Conv)      | 因果性保证、可并行推理；参数相对少               | 覆盖范围小，需堆叠多层并带膨胀卷积扩大感受野      | 需要在线生成、逐帧预测场景              | 使用Dilated Conv增加感受野；结合跳跃连接或Transformer层获取长程信息 |
| **Transformer Encoder**      | 强大的全局依赖捕捉能力，灵活，可处理任意长度      | 计算/显存开销大，对数据需求高                 | 长视频生成、高级特征抽取             | 按帧展开序列输入或时空分块；位置编码设计（如分离时空编码）；利用稀疏注意力优化  |
| **空间/时间注意力分离**    | 专门提取空间 vs 时间信息，易解释               | 需要设计注意头分类策略，增加模型复杂度           | 对运动细节与内容均需关注的任务        | 将注意力头分为空间头和时间头；或在线分析确定头类型【53†L108-L117】 |
| **对比学习 (LSTCL)**       | 无需标签，提升时序一致性，泛化能力强            | 需预训练阶段，难以与主任务同时优化             | 表征预训练或少标注数据场景            | 构造长短序列对比损失；适配SimCLR/BYOL等框架；可结合Transformer或CNN   |
| **动态稀疏训练 (DynST)** | 减少计算与参数，动态剪枝              | 风险丢失重要信息，训练需精心调度              | 资源受限情况下的轻量模型           | 在训练中定期计算重要性排序并剪枝弱特征【60†L42-L50】；使用稀疏更新算法 |

```mermaid
graph LR
  A[视频序列输入] --> B[编码器 (时空卷积/Transformer)]
  B --> C[稀疏瓶颈激活 (SAE 特征)]
  C --> D[解码器 (时空反卷积/Transformer)]
  D --> E[视频重构输出]
  B --> F[解释/对齐模块]
  F --> C
```
*图：时序SAE的高层设计示意。视频输入先经时空编码器生成稀疏激活特征，然后经解码器重构。可解释模块（基于SAE激活）用于提供人类对齐的控制信号。*

## 3. 相关论文与比较

下表列出了与SAE可解释及视频生成控制相关的代表性论文（近三年为主），并比较了它们的属性：

| 论文及作者                                      | 年份 | 方法类别             | 针对视频? | 激活干预?       | 开源代码   |
|----------------------------------------------|-----|-------------------|---------|---------------|--------|
| Huang 等, **TIDE (Temporal-aware SAE)**【38†L57-L64】      | 2025 | SAE解释（图像DiT）      | 否（图像）  | 否             | 待查    |
| Rao 等, **Discover-then-Name-CBM**【44†L73-L82】 | 2024 | SAE概念发现（CLIP）    | 否（多模态图像） | 否             | 是【44†L73-L82】 |
| Pach 等, SAE (VLM可解释)【49†L239-L247】             | 2025 | SAE解释（CLIP、VLM）   | 否（视觉语言） | 否(间接控制LLM)  | 是     |
| Helbling 等, **ConceptAttention**【9†L21-L29】  | 2025 | 概念显著图（DiT）       | 部分（图+视频) | 是（定位概念）    | 是【9†L21-L29】   |
| Hwang 等, **IMAP**【8†L139-L148】              | 2026 | 运动显著图（VDiT）     | 是      | 否（可观察运动）   | 未知   |
| Poppi 等, **CounterVid**【16†L61-L69】        | 2026 | 反事实视频生成       | 是      | 否（对齐辅助）     | 未知   |
| Ye 等, **T2VUnlearning**【23†L59-L68】        | 2025 | 概念擦除（T2V微调）    | 是      | 是（擦除不良概念）  | 是【23†L59-L68】   |
| Zhang 等, **Align4Gen**【47†L121-L129】      | 2025 | 多特征对齐（训练）     | 是      | 否（训练改进）     | 未知   |
| Jeong 等, **Track4Gen**【57†L55-L63】        | 2025 | 点跟踪监督（CVPR2025） | 是      | 否（时序一致性）   | 是     |
| Du 等, **SparseVideoGen**【53†L108-L117】    | 2025 | 稀疏注意力加速       | 是      | 否（效率优化）     | 未知   |
| **早期工作：**                                   |     |                   |         |               |        |
| SlowFast (Feichtenhofer 2019)                | 2019 | 双流网络（高低帧率）    | 是      | 否             | 否     |
| U-Net 3D (Blattmann 2023)                    | 2023 | U-Net3D 卷积        | 是      | 否             | 否     |

*表：近三年相关文献比较。方法类别包括解释方法（SAE、显著图、反事实等）和对齐/控制策略（干预、训练对齐、擦除等）。“激活干预”列标注是否利用内部激活（注意力/特征）对模型输出施加了控制。*

## 4. 可操作的实现建议

基于以上分析，我们提出以下可实施的实验方案，适用于有限计算资源环境：

1. **在小规模视频数据上训练时序SAE**：选取公开视频数据集（如UCF101或Kinetics-100的子集）以及现成的轻量级视频扩散模型（例如使用较小参数的Stable Video Diffusion）。构造一个时序SAE：编码器可选用3D卷积或ConvLSTM，瓶颈施加L1稀疏性约束，解码器对帧序列重构。训练时优化重构误差加上激活稀疏度损失。评估指标：重构质量（PSNR、LPIPS）和可解释性（如对瓶颈特征做聚类，验证是否对应运动、物体等语义）。成功标准：在可接受的重构质量下，SAE提取的特征应展现出可区分的语义结构。潜在风险：视频SAE训练成本高（可能需多卡），以及解释性评估主观，可通过增加视频分类器准确率检验特征有效性来缓解。

2. **概念显著图驱动的可控生成**：利用**ConceptAttention**或**IMAP**方法，将其应用到一个视频扩散模型生成流程中。例如，选择一个高层概念（如“汽车”或不良内容概念“暴力”），使用概念注意力在生成过程（或生成后关键帧）定位该概念区域。然后尝试对该区域进行干预：一种策略是根据显著图屏蔽或扭曲对应的潜在空间表示；另一种是修改文本提示（加入否定词）并结合显著信息迭代生成。数据集可基于MS-COCO/Video或合成Prompt。评估：验证干预前后目标概念出现的频率（人工标注或自动检测器评估）、生成质量（FID）及对原提示语义的一致性。成功标准：经过干预后，目标概念的生成明显减少且视频语义保持连贯。风险在于显著图可能误识别区域或覆盖过多，需调参显著阈值和控制策略。

3. **SAE控制性实验（VLM编码器干预）**：基于Pach等的思路【49†L239-L247】，在CLIP或其他视觉编码器（与视频模型共享编码）上训练SAE，找到若干单语义方向。然后通过人工选择或简单规则改变这些方向（如降低“性感”特征激活）并送入多模态模型（如VideoLLM或LLaVA）进行生成。数据集可选用公开图像/视频数据。评估：观察修改后的生成结果中相关概念是否被削弱。成功标准：无需修改语言模型，仅SAE干预即可明显改变输出语义方向。风险：需要事先验证SAE特征含义，对复杂场景难以直接作用。

4. **视频模型点跟踪一致性测试**：重现Track4Gen【57†L55-L63】中的思想：在视频扩散模型训练或后处理时，引入点跟踪损失以改善时序一致性（例如对生成连续帧中的一些追踪点加一致性约束）。使用Stable Video Diffusion，并选择小规模视频跟踪数据（如KITTI中的点轨迹）。评估：对比有无跟踪损失下的生成视频，使用人眼或自动跟踪算法评估帧间物体位置漂移程度（下标误差）和视觉质量。成功标准：跟踪一致性显著提升（漂移误差降低），同时视频质量无显著退化。风险：添加跟踪损失增加训练复杂度，需要预先获得真实视频的轨迹数据。

5. **稀疏注意力加速与分析**：基于SparseVideoGen【53†L108-L117】的发现，对现有视频DiT模型进行稀疏注意力测试。实验方法：在推理时为注意力头动态选择稀疏模式（空间型或时间型），并跳过大部分无关token（如只关注关键对象或中心token）。数据集：短视频集（如UCF-101的验证集），模型：CogVideo小版本。评估：比较稀疏与全注意力下的视频质量（FID/LPIPS）和生成时间。成功标准：明显降低计算量（时间加速），且视频质量仅小幅下降。风险：若稀疏选择失误可能造成显著画质下降，可通过在线采样或小批评估错误率动态调整。

每个方案都应设定清晰的成功标准（如质量指标阈值、概念检测率变化等）并评估潜在风险（特征丢失、误控制、计算过载等），为人类对齐和解释性研究提供可执行验证路径。

## 5. 检索策略与优先来源

本报告主要检索近三年（2023–2026）的学术论文与预印本，优先使用**arXiv**和各大会议（CVPR/ICCV/NeurIPS/ICML/ICLR/AAAI/ACL）及Google Scholar。检索关键词包括：“Video Diffusion Transformer interpretability”、“Sparse Autoencoder diffusion”、“Temporal autoencoder video”、“vision transformer sparse features”、“video counterfactual diffusion”等。我们还关注顶尖实验室和企业博客（OpenAI、Google Research、Meta、DeepMind等）发布的技术报告。检索时参照“标题+关键词+年份”组合，并根据摘要筛选相关度高的文献。对于中文资料，也尝试使用相关中文关键词在数据库中搜索。检索过程中注重引用官方版本（arXiv、会议论文PDF等），并优先阅读**核心论文**：如Huang等的TIDE【38†L57-L64】、Rao等的DN-CBM【44†L73-L82】、Pach等的SAE VLM海报【49†L239-L247】、ConceptAttention【9†L21-L29】和IMAP【8†L139-L148】等，以获取第一手细节。Google Scholar 和 Papers with Code 也用于确认最新论文和代码链接。

## 6. 结论

综上所述，目前未见直接针对**视频DiT内部激活的SAE解释**工作，但已有多种相关方法可供借鉴：包括图像生成领域的TIDE【38†L57-L64】、视觉-语言领域的Rao等【44†L73-L82】和Pach等【49†L239-L247】等，以及视频生成中的注意力显著图技术（ConceptAttention【9†L21-L29】、IMAP【8†L139-L148}等）。要将SAE应用于视频生成模型，需要考虑时空特性，引入时序模块（3D卷积、时序Transformer、ConvLSTM等）并处理大规模时序数据。此外，可解释信号已经被用于指导模型对齐（如注意力编辑【3†L47-L52】、故障检测【67†L19-L27】、概念擦除【23†L59-L68】等），证明可解释性在控制生成中具有潜力。未来工作可沿着提出的建议实施实验，系统评估SAE在视频域的表现及其对模型行为的干预能力，从而推动视频生成模型的透明度和安全性。  

